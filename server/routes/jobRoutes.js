// Job routes: GET /, GET /:id, POST / (HR), PUT /:id (HR), DELETE /:id (HR)
const express = require('express');
const router = express.Router();

// TODO: implement job CRUD handlers (jobs feature).

module.exports = router;
