// Interview routes: POST / (HR), GET / (HR)
const express = require('express');
const router = express.Router();

// TODO: implement interview handlers (interviews feature).

module.exports = router;
